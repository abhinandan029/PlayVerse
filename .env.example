# Used by `npm run dev`. An empty value makes the frontend use Vite's /api proxy.
VITE_DEV_BACKEND_URL=http://localhost:3000

# Used by `npm run build` and `npm run preview`.
VITE_PROD_BACKEND_URL=https://your-production-backend.example.com